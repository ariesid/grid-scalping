# ---- KREDENSIAL (WAJIB ISI) ----
GATE_API_KEY=bb55e310fc8c2fedec60b13dba952bd6
GATE_API_SECRET=94cb60618daa51c06454ef5cf38e29f5e8773d5619dd4fce73de1d7043b96fa2

# ---- LINGKUNGAN ----
USE_TESTNET=0          # 1 = testnet, 0 = mainnet (SAMAKAN dengan asal key Anda)

# ---- PARAMETER BOT ----
SYMBOL=SOL_USDT
GATE_ACCOUNT=spot

# Option 1: Manual range (if both LOWER and UPPER are set, use these)
# LOWER=85000
# UPPER=95000

# Option 2: Auto range based on current price percentage (RECOMMENDED)
AUTO_RANGE_PERCENT=5   # Bot akan set range = current_price ± 5%
# Contoh: Price 90000 dengan 5% → range 85500 - 94500

GRIDS=5                # Jumlah grid levels (5 = 5 orders aktif di awal)
QUOTE_PER_ORDER=99    # Modal per order dalam USDT (100 USDT per transaksi)
GEOMETRIC=1            # 0 = Arithmetic spacing (jarak tetap), 1 = Geometric spacing (% tetap)
POLL=5                 # Interval cek status order dalam detik (5s = cek setiap 5 detik)
STATE_FILE=grid_state.json
